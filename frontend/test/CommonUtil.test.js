import { expect } from 'chai';
import {
  doSort
} from 'CommonUtil/CommonUtil.js';


describe('UNIT/COMMONUTIL - CommonUtil:doSort function', () => {

  it('should sort asc', () => {
    //todo

    expect(true).to.equal(true)
  });

  it('should sort desc', () => {
     //todo
    expect(true).to.equal(true)
  });

  it('should not sort', () => {
     //todo
    expect(true).to.equal(true)
  });

});
